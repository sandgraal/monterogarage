# deps / build
node_modules/
dist/
# The configured-build audit target (`ci.yml`, `tests/e2e/garage-unreachable.spec.ts`).
dist-configured/
.astro/
coverage/
playwright-report/
test-results/
# Runtime scratch dir for tests/repo-hygiene/no-control-characters.test.ts's
# mutation-test-the-probe-corpus cases, which write and remove real control
# bytes on disk inside the scanned tree. Cleaned up by the test itself
# (afterEach) even on failure; ignored here as a second line of defense.
tests/repo-hygiene/__scratch__/

# Vercel CLI scratch: `.vercel/project.json` carries the org and project IDs
# this checkout is linked to, plus pulled environment values. Never committed.
.vercel/

# env & secrets
.env
.env.*
!.env.example

# local tooling
.claude/settings.local.json
.claude/worktrees/
*.local

# The `npm run gaps` content-backlog report (T703, GAP-01) — a generated
# artifact, uploaded by CI, never committed.
/gaps-report.json

# OS
.DS_Store
